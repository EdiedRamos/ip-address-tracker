import { Wrapper, Input, SearchButton } from "../";

export const Search = () => {
  return (
    <Wrapper>
      <Input placeholder="Search for any IP address or domain" />
      <SearchButton />
    </Wrapper>
  );
};
