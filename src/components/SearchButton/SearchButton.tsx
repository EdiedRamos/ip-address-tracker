import { Button } from "..";
import { ArrowIcon } from "../../assets/images";

export const SearchButton = () => {
  return (
    <Button>
      <img src={ArrowIcon} width="10px" />
    </Button>
  );
};
